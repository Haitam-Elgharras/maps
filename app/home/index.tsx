import { Component } from "react";
import { View, Text } from "react-native";
import CameraComponent from "../(tabs)/cameraComponent";

export class index extends Component {
    render() {
        return (
            <View>
                <CameraComponent />
            </View>
        )
    }
}

